# Web deployment dependencies (trimmed for Railway/Render/Fly.io)
#
# Removed from full requirements.txt:
#   tensorflow  -- dead code, only PyTorch is used for ML inference
#   docker      -- not needed in simulated execution mode
#   matplotlib  -- server-side plotting not needed in web mode
#   seaborn     -- server-side plotting not needed in web mode
#   scikit-learn -- not used at runtime
#   jupyter / ipykernel -- development tools only
#   tqdm        -- progress bars for CLI scripts only
#
# PyTorch: install CPU-only variant (~200 MB vs ~2 GB with CUDA).
# Railway/Render build step should run:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# before installing this file, OR add the --extra-index-url below.

psutil==7.0.0
pandas>=1.3.0
numpy>=1.21.0
flask==3.1.1
flask-cors==6.0.1
requests>=2.28.0
cvxpy==1.7.3
osmnx==1.9.4
shapely>=2.0.0
networkx>=2.8

# PyTorch CPU-only — uncomment if not installed separately
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch>=2.0.0
